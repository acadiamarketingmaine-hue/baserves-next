# For the booking-site session (escape.baserves.com)

Found by the baserves.com UX pass on 2026-09-23. **Nothing here was changed.** The booking repo is yours.
Screenshots are the `6x-handoff-*` files in this folder.

| # | Sev | What a guest sees | Where | Suggested fix |
|---|-----|-------------------|-------|---------------|
| B1 | S1 | **No way back to baserves.com.** On `/`, `/chief-noonday-outdoor-center` and `/long-lake-outdoor-center`, both header logos link to `escape.baserves.com`. The only baserves.com link is a `mailto:yankee@baserves.com`. The contact page (`app/contact/page.tsx:75`) is the one exception. The brand site opens escape.* in a new tab, so the old tab is the only way back. | header / `SiteFooter.tsx` | Add a "baserves.com" or "About this park" link in the header or footer that goes to the matching brand page (e.g. `https://baserves.com/long-lake-outdoor-center`). |
| B2 | S1 | **"MOTEL" / "MOTEL_ROOM" badges.** `{unit.type}` is printed raw in a badge. Tailwind `capitalize` doesn't lowercase, so guests see enum text. Washington State Park units whose names contain "lodge", "room" or "hotel" were filed as `MOTEL_ROOM` (`scripts/import-washington.mjs:64-66`). Washington has no motel. | `components/BAServicesHomepage.tsx:916-918` | Map the enum to a human label ("Cabin", "Motel room"), and re-check Washington's unit types. |
| B3 | S2 | **The home-page booking widget pulls a 2.3 MB photo.** baserves.com embeds `escape.baserves.com/embed/widget.js`, which loads `…blob.vercel-storage.com/unit/cmkcrmi4i0001nn4fjxbuzlcp/1781151219397-dmn2my.jpg` (2,293 KB) at full size. That's most of the brand home page's phone payload. | `embed/widget.js` image path | Serve widget images resized (e.g. through `/_next/image` with `w=640`) or resize on upload. |
| B4 | S2 | **Widget carousel arrows have no accessible name.** The `button.baserves-nav-btn.baserves-prev/next` buttons contain only an SVG. There are 4 of them on the baserves.com home page. | `embed/widget.js` | Add `aria-label="Previous"` / `"Next"`. |
| B5 | S2 | **Escape root is a 4-property picker, but the brand site sends every generic "Book" there.** Root lists Chief Noonday, Long Lake, Meramec and Washington only. A guest who clicked "Book Your Adventure" after reading about West Virginia, Indiana or Rhode Island lands on a page that can't book those. The brand-side decision is logged in `decisions.md` (D1). | `/` | Consider a line on root: "Camping in a national forest? Those sites book on Recreation.gov — see baserves.com/experiences." |
| B6 | S3 | **No UTM or referrer hand-off.** Brand links carry no `?utm_source=baserves.com`, so booking analytics can't tell brand-site traffic from direct traffic. | n/a | If you want this, say which params you read and the brand side will add them. |
| B7 | S3 | Carried over from your own audit and still live on 2026-09-23: escape.* `/meramec-state-park` and `/washington-state-park` are the scraped mostateparks copies. | booking footer | See your audit, finding 2. |
